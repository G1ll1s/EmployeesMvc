@model Employee[]

@{
    ViewBag.Title = "Alla Anställda!";
}

<h3>Anställda:</h3>
@foreach (var employee in Model)
{
    <li>
        <a asp-controller="employees" asp-action="details" asp-route-id="@employee.Id">
            @employee.Name - @employee.Email
        </a>
    </li>
}


